# Private AI instructions
_skills/

# Signed contracts and agreements — never commit
_contracts/
.env
.env.local
*.env

# Private config
/instructions/
/prompts/

# System files
.DS_Store
node_modules/

# Downloaded image assets (served from R2, not committed to repo)
docs/**/images/
_download_report.txt
download_service_images.py
wire_service_images.py

# ── Internal notes — live ONLY in the PRIVATE repo: thomasfencear/ThomasFence-Private ──
# This is the PUBLIC website repo. Never commit memory, sessions, client comms,
# reports, SOPs, task lists, or working inventories here. (Purged from history June 23, 2026.)
memory/
sessions/
/greg-*
/session-summary-*
/notion-reorg-*
/_sop-*
/TASK-LIST.txt
/*.csv
# ── AI instructions & local config — PUBLIC repo, never commit these ──
# (Removed from history June 25, 2026 — were exposing build methodology + local paths)
.claude/
CLAUDE.md
